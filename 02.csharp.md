### 희소 행렬 

- [SparseMatrix](./Chapter3/Example-03-18-C#/Project/Program.csx)
- sparse matrix 
  - 0 값을 지니는 요소가 전체 행렬에서 차지하는 비중이 클 때 사용하는 행렬. 
  - 희소 행렬은 `SparseMat` 클래스로 표현, Mat 클래스와 유사성이 많고 동일한 타입을 지원 일부 매서드를 동일한 방식으로 사용할 수 있다. 
  - SparseMat은 행렬 표현식은 지원하지 않으며, 행렬 표현식은 `MatExpr` 클래스로 지원된다.  
  - 실제 존재하는 값에만 메모리를 할당하므로 해시 태이블을 이용해 0이 아닌 요소만 저장.  
  - 희소 행렬은 행렬의 크기 대신 차원(dimensionality)를 사용, 차원의 크기에 따라 해시 테이블의 크기를 정의 한다. 

- [SparseMatrix 희소 행렬의 요소 할당 및 접근](./Chapter3/Example-03-19-C#/Project/Program.csx)  
- [Mat클래스 관심 영역](./Chapter3/Example-03-20-C#/Project/Program.csx)

#### Continuous 하지 않는 이유

`roi3`가 `IsContinuous=False`인 이유는 **ROI가 원본 Mat의 일부만 잘라낸 뷰(view)** 이기 때문입니다.

핵심은 이겁니다:

- 원본 `m`은 한 행이 `1920 * 3` 바이트만큼 연속 저장됩니다.
- `roi3 = m.SubMat(100, 300, 200, 300)` 는 **가운데 부분만** 잘라낸 행렬입니다.
- 그래서 `roi3`의 한 행이 끝난 뒤, 다음 행으로 넘어갈 때는 `roi3` 크기만큼이 아니라 **원본 행 폭(step)** 만큼 건너뛰어야 합니다.
- 즉 `roi3` 내부 데이터가 **자기 기준으로 연속된 덩어리**가 아니어서 `IsContinuous=False`가 됩니다.

비교하면:

- `roi2`도 `IsContinuous=False`
- `roi2`는 좌상단에서 잘랐기 때문에 `Data`가 원본 시작 주소와 같아 보일 수 있지만, 여전히 원본의 큰 행 폭을 공유하므로 연속 메모리는 아닙니다.
- `roi3`는 가운데에서 잘랐기 때문에 `Data` 자체도 원본 시작점이 아니라 중간 주소를 가리킵니다.

쉽게 말하면:

- `Mat` 전체: 각 행이 붙어서 저장될 수 있음
- `ROI/SubMat`: 원본 행 구조를 그대로 물려받기 때문에 보통 연속 메모리가 아님

예외적으로는 ROI를 **복사해서 새 Mat으로 만들면** 연속이 될 수 있습니다.

```csharp
Mat continuous = roi3.Clone();
```

이렇게 하면 `continuous`는 새 메모리를 가지므로 보통 `IsContinuous=True`가 됩니다.


## 관심 채널 

