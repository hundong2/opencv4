## camera connection

- [Camera connection](.\Chapter4\Example-04-09-C#\Project\ProgramForWindow.csx)  
- 카메라 번호는 0~ 부터 시작. 

## Image connection

- 이미지 연결은 서로 다른 이미지를 이어 붙여 하나의 이미지로 만드는 기능을 한다.  
- [Image connect for csharp](.\Chapter4\Example-04-11-C#\Project\ProgramForWindow.csx)  
- 행이나 열중 하나의 차원이라도 동일하지 않다면 이미지를 연결 할 수 없다.  

## 도형 그리기 

### 선형 타입

OpenCV에서 도형(선, 원, 사각형 등)을 그릴 때 `lineType` 파라미터로 선의 렌더링 방식을 지정한다.

| 상수 | 값 | 설명 |
|------|-----|------|
| `LineTypes.Link4` | 4 | 브레젠험 알고리즘 – **4-연결** (상하좌우 4방향 픽셀 연결) |
| `LineTypes.Link8` | 8 | 브레젠험 알고리즘 – **8-연결** (대각선 포함 8방향 픽셀 연결, 기본값) |
| `LineTypes.AntiAlias` | 16 | **안티 에일리어싱** – 가우시안 필터로 계단 현상(jagged edge)을 부드럽게 처리 |
| `LineTypes.Filled` | -1 | **내부 채우기** – 선이 아닌 도형 내부를 채움 (두께 파라미터 무시) |

- **브레젠험 알고리즘**: 정수 연산만으로 직선을 근사하는 래스터화 알고리즘.  
  - 4-연결은 픽셀이 수직·수평으로만 이어져 선이 더 굵어 보이고,  
  - 8-연결은 대각선도 허용하여 더 자연스러운 선을 생성. 일반적으로 8-연결을 기본값으로 사용.
- **안티 에일리어싱 (Anti-Aliasing)**: 경계 픽셀에 중간 밝기값을 부여해 선이 부드럽게 보이도록 처리. 연산 비용이 상대적으로 높다.
- **내부 채우기 방식 (Filled)**: `thickness = -1` 또는 `LineTypes.Filled` 지정 시 도형의 내부를 지정한 색상으로 채운다. 원, 타원, 다각형 등에 적용 가능.

### 함수 시그니처 및 파라미터

#### `Cv2.Line` – 직선 그리기

```
Cv2.Line(Mat img, Point pt1, Point pt2, Scalar color, int thickness=1, LineTypes lineType=LineTypes.Link8, int shift=0)
```

| 파라미터 | 설명 |
|----------|------|
| `img` | 대상 이미지 (Mat) |
| `pt1` | 시작점 좌표 |
| `pt2` | 끝점 좌표 |
| `color` | 선 색상 (BGR 순서, `Scalar(B, G, R)`) |
| `thickness` | 선 두께 (픽셀 단위, 기본값 1) |
| `lineType` | 선형 타입 (`Link4`, `Link8`, `AntiAlias`) |
| `shift` | 좌표 소수점 비트 수 (기본값 0) |

```csharp
// 빨간색(0,0,255) 직선, 두께 3, 안티 에일리어싱
Cv2.Line(img, new Point(100, 100), new Point(1200, 100), new Scalar(0, 0, 255), 3, LineTypes.AntiAlias);
```

---

#### `Cv2.Circle` – 원 그리기

```
Cv2.Circle(Mat img, Point center, int radius, Scalar color, int thickness=1, LineTypes lineType=LineTypes.Link8, int shift=0)
```

| 파라미터 | 설명 |
|----------|------|
| `img` | 대상 이미지 |
| `center` | 원의 중심 좌표 |
| `radius` | 반지름 (픽셀 단위) |
| `color` | 색상 (BGR) |
| `thickness` | 두께. `-1` 또는 `Cv2.FILLED` 지정 시 내부 채움 |
| `lineType` | 선형 타입 |

```csharp
// 초록색(0,255,0) 원, 반지름 50, 내부 채움, 4-연결
Cv2.Circle(img, new Point(300, 300), 50, new Scalar(0, 255, 0), Cv2.FILLED, LineTypes.Link4);
```

---

#### `Cv2.Rectangle` – 사각형 그리기

```
Cv2.Rectangle(Mat img, Point pt1, Point pt2, Scalar color, int thickness=1, LineTypes lineType=LineTypes.Link8, int shift=0)
```

| 파라미터 | 설명 |
|----------|------|
| `img` | 대상 이미지 |
| `pt1` | 사각형 좌상단 좌표 |
| `pt2` | 사각형 우하단 좌표 |
| `color` | 색상 (BGR) |
| `thickness` | 테두리 두께. `-1` 지정 시 내부 채움 |
| `lineType` | 선형 타입 |

```csharp
// 파란색(255,0,0) 사각형, 두께 5
Cv2.Rectangle(img, new Point(500, 200), new Point(1000, 400), new Scalar(255, 0, 0), 5);
```

---

#### `Cv2.Ellipse` – 타원(호) 그리기

```
Cv2.Ellipse(Mat img, Point center, Size axes, double angle, double startAngle, double endAngle, Scalar color, int thickness=1, LineTypes lineType=LineTypes.Link8, int shift=0)
```

| 파라미터 | 설명 |
|----------|------|
| `img` | 대상 이미지 |
| `center` | 타원 중심 좌표 |
| `axes` | 반축 크기 `Size(장축반경, 단축반경)` |
| `angle` | 타원 전체 회전각 (도, 시계 방향) |
| `startAngle` | 호의 시작 각도 (도) |
| `endAngle` | 호의 끝 각도 (도). `0~360` 이면 완전한 타원 |
| `color` | 색상 (BGR) |
| `thickness` | 두께. `-1` 지정 시 내부 채움 |
| `lineType` | 선형 타입 |

```csharp
// 노란색(255,255,0) 타원, 반축(100,50), 회전 0°, 90°~180° 호만 그리기, 두께 2
Cv2.Ellipse(img, new Point(1200, 300), new Size(100, 50), 0, 90, 180, new Scalar(255, 255, 0), 2);
```

---

#### `Cv2.Polylines` – 다각형(폴리라인) 그리기

```
Cv2.Polylines(Mat img, IEnumerable<IEnumerable<Point>> pts, bool isClosed, Scalar color, int thickness=1, LineTypes lineType=LineTypes.Link8, int shift=0)
```

| 파라미터 | 설명 |
|----------|------|
| `img` | 대상 이미지 |
| `pts` | 꼭짓점 좌표 배열의 배열 (`Point[][]` 또는 `IEnumerable<IEnumerable<Point>>`). 여러 개의 폴리라인을 한 번에 그릴 수 있다 |
| `isClosed` | `true`: 마지막 점과 첫 번째 점을 연결해 닫힌 다각형 생성. `false`: 열린 폴리라인 |
| `color` | 선 색상 (BGR) |
| `thickness` | 선 두께 |
| `lineType` | 선형 타입 |
| `shift` | 좌표 소수점 비트 수 (기본값 0) |

> `Cv2.Polylines`는 내부 채우기(`thickness = -1`)를 지원하지 않는다.  
> 내부를 채우려면 `Cv2.FillPoly` 또는 `Cv2.FillConvexPoly`를 사용한다.

```csharp
// 꼭짓점 배열 정의
Point[] pts1 = new Point[]
{
    new Point(100, 100),
    new Point(300, 50),
    new Point(500, 100),
    new Point(400, 300),
    new Point(200, 300),
};

// 청록색(0,255,255) 닫힌 다각형, 두께 2
Cv2.Polylines(img, new Point[][] { pts1 }, true, new Scalar(0, 255, 255), 2);
```

---

#### `Cv2.FillPoly` – 다각형 내부 채우기

```
Cv2.FillPoly(Mat img, IEnumerable<IEnumerable<Point>> pts, Scalar color, LineTypes lineType=LineTypes.Link8, int shift=0, Point? offset=null)
```

| 파라미터 | 설명 |
|----------|------|
| `img` | 대상 이미지 |
| `pts` | 꼭짓점 좌표 배열의 배열 (`Point[][]`). 복수의 다각형을 한 번에 채울 수 있다 |
| `color` | 채울 색상 (BGR) |
| `lineType` | 경계선의 선형 타입 |
| `shift` | 좌표 소수점 비트 수 (기본값 0) |
| `offset` | 모든 꼭짓점에 적용할 오프셋 좌표 (선택, 기본값 없음) |

> `Cv2.Polylines`와 달리 내부를 색상으로 채운다.  
> 오목(concave) 다각형도 처리 가능. 볼록(convex) 다각형만 채울 때는 더 빠른 `Cv2.FillConvexPoly`를 사용한다.

```csharp
Point[] pts2 = new Point[]
{
    new Point(600, 100),
    new Point(900, 50),
    new Point(1100, 200),
    new Point(950, 400),
    new Point(650, 380),
};

// 마젠타색(255,0,255) 내부 채움, 안티 에일리어싱 경계
Cv2.FillPoly(img, new Point[][] { pts2 }, new Scalar(255, 0, 255), LineTypes.AntiAlias);
```

---

#### `Cv2.PutText` – 텍스트 그리기

```
Cv2.PutText(Mat img, string text, Point org, HersheyFonts fontFace, double fontScale, Scalar color, int thickness=1, LineTypes lineType=LineTypes.Link8, bool bottomLeftOrigin=false)
```

| 파라미터 | 설명 |
|----------|------|
| `img` | 대상 이미지 |
| `text` | 출력할 문자열 |
| `org` | 텍스트 **왼쪽 하단** 기준점 좌표 |
| `fontFace` | 폰트 종류 (`HersheyFonts` 열거형). `\|` 연산자로 스타일 조합 가능 |
| `fontScale` | 폰트 크기 배율 (기본 크기에 대한 배수) |
| `color` | 텍스트 색상 (BGR) |
| `thickness` | 텍스트 획 두께 |
| `lineType` | 선형 타입 |
| `bottomLeftOrigin` | `true`이면 좌표 원점을 이미지 좌하단으로 설정 (기본값 `false` = 좌상단) |

**주요 `HersheyFonts` 값**

| 상수 | 설명 |
|------|------|
| `HersheyFonts.HersheySimplex` | 기본 산세리프체 |
| `HersheyFonts.HersheyComplex` | 세리프체 |
| `HersheyFonts.HersheyDuplex` | 이중선 산세리프체 |
| `HersheyFonts.HersheyTriplex` | 삼중선 세리프체 |
| `HersheyFonts.Italic` | 이탤릭 스타일 (다른 폰트와 `\|` 조합하여 사용) |

```csharp
// 흰색 "OpenCV" 텍스트, HersheyComplex+Italic 폰트, 배율 2.0, 두께 3
Cv2.PutText(img, "OpenCV", new Point(900, 600), HersheyFonts.HersheyComplex | HersheyFonts.Italic, 2.0, new Scalar(255, 255, 255), 3);
```

## Trackbar 

- [Track Bar](.\Chapter4\Example-04-17-C#\Project\ProgramForWindow.csx)  

## Image Save 

- [Save Image](.\Chapter4\Example-04-18-C#\Project\ProgramForWindow.csx)   

## Video 

- []()  