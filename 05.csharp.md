## 이미지 변형 

- [CvtColor](.\Chapter5\Example-05-01-C#\Project\ProgramForWindow.csx)  

```csharp
Cv2.CvtColor(src, dst, ColorConversionCodes.BGR2GRAY);
```

- `HSV` 다중 채널 색상 이미지
    - Hue, Saturation, Value의 약자로서 각각 색상, 채도, 명도를 의미한다.  

- `ImShow` 함수는 기본적으로 BGR의 색상 패턴으로 표현, 그러므로, HSV의 색상 공간은 시각적으로 구별하기 힘들다. 

## HSV 색상 공간

HSV는 사람이 색을 인식하는 방식에 가깝게 설계된 색상 모델이다.  
BGR/RGB처럼 빛의 혼합 비율로 색을 표현하는 대신, **"어떤 색인가 / 얼마나 선명한가 / 얼마나 밝은가"** 세 가지 축으로 색을 기술한다.

### 세 가지 축 직관적 이해

```
        흰색 (White)
          │  ↑ Value(명도) 높음
          │
          │         ← Saturation(채도) →
          │   회색────────────────── 순색(빨강/초록/파랑 등)
          │
          ↓ Value 낮음
        검정 (Black)
```

| 축 | 범위 (OpenCV) | 의미 | 직관적 비유 |
|----|--------------|------|------------|
| **H (Hue, 색상)** | 0 ~ 179 | 색의 종류 (빨강·노랑·초록·파랑…) | 물감 팔레트에서 어떤 색을 고를지 |
| **S (Saturation, 채도)** | 0 ~ 255 | 색의 선명도·진함 | 0이면 무채색(회색), 255이면 가장 순수한 색 |
| **V (Value, 명도)** | 0 ~ 255 | 색의 밝기 | 0이면 완전한 검정, 255이면 가장 밝음 |

> **OpenCV의 Hue 범위는 0~179** (일반적인 0°~360°를 절반으로 압축).  
> 예: 빨강 ≈ 0 or 179, 초록 ≈ 60, 파랑 ≈ 120

### BGR과 비교

| 특성 | BGR | HSV |
|------|-----|-----|
| 특정 색 검출 | 어려움 (R·G·B 모두 조건 필요) | 쉬움 (H 범위만 지정) |
| 조명 변화 대응 | 민감 | V만 변하므로 H·S는 안정적 |
| 사람 직관 | 낮음 | 높음 |

### 활용 예시 (빨간 물체 검출)

```csharp
// BGR → HSV 변환
Cv2.CvtColor(src, hsv, ColorConversionCodes.BGR2HSV);

// 빨간색 범위 마스크 (Hue가 0~10 또는 170~179)
Mat mask1 = new Mat(), mask2 = new Mat();
Cv2.InRange(hsv, new Scalar(0, 100, 100), new Scalar(10, 255, 255), mask1);
Cv2.InRange(hsv, new Scalar(170, 100, 100), new Scalar(179, 255, 255), mask2);
Mat mask = mask1 | mask2;
```

---

## HSV 변환 알고리즘

### BGR → HSV 수학적 변환 절차

BGR 값을 먼저 정규화한 뒤 H·S·V를 계산한다.

**Step 1. 정규화**

$$
R' = \frac{R}{255},\quad G' = \frac{G}{255},\quad B' = \frac{B}{255}
$$

$$
C_{max} = \max(R', G', B'), \quad C_{min} = \min(R', G', B'), \quad \Delta = C_{max} - C_{min}
$$

**Step 2. Hue (색상) 계산**

$$
H = \begin{cases}
60° \times \left(\dfrac{G'-B'}{\Delta} \bmod 6\right) & \text{if } C_{max} = R' \\
60° \times \left(\dfrac{B'-R'}{\Delta} + 2\right) & \text{if } C_{max} = G' \\
60° \times \left(\dfrac{R'-G'}{\Delta} + 4\right) & \text{if } C_{max} = B' \\
0° & \text{if } \Delta = 0 \text{ (무채색)}
\end{cases}
$$

> OpenCV는 8비트 저장을 위해 $H$ 값을 **2로 나누어** 0~179 범위로 저장한다.

**Step 3. Saturation (채도) 계산**

$$
S = \begin{cases}
\dfrac{\Delta}{C_{max}} & \text{if } C_{max} \neq 0 \\
0 & \text{if } C_{max} = 0
\end{cases}
\quad \xrightarrow{\times 255} \quad S_{opencv} \in [0, 255]
$$

**Step 4. Value (명도) 계산**

$$
V = C_{max} \quad \xrightarrow{\times 255} \quad V_{opencv} \in [0, 255]
$$

---

### 주요 색상별 Hue 범위 (OpenCV 기준, 0~179)

> OpenCV H 범위 = 색상환 각도(°) ÷ 2

| 색상 | 대표 색 | 색상환 각도 | OpenCV H 범위 | InRange 예시 (S≥50, V≥50) |
|------|--------|------------|--------------|--------------------------|
| 빨강 (Red) 하단 | 🔴 | 0°~10° | **0 ~ 5** | `Scalar(0,50,50)` ~ `Scalar(5,255,255)` |
| 빨강 (Red) 상단 | 🔴 | 350°~360° | **170 ~ 179** | `Scalar(170,50,50)` ~ `Scalar(179,255,255)` |
| 오렌지 (Orange) | 🟠 | 15°~45° | **8 ~ 22** | `Scalar(8,50,50)` ~ `Scalar(22,255,255)` |
| 노랑 (Yellow) | 🟡 | 45°~75° | **22 ~ 38** | `Scalar(22,50,50)` ~ `Scalar(38,255,255)` |
| 연두 (Yellow-Green) | 🟢 | 75°~105° | **38 ~ 52** | `Scalar(38,50,50)` ~ `Scalar(52,255,255)` |
| 초록 (Green) | 🟢 | 105°~135° | **52 ~ 67** | `Scalar(52,50,50)` ~ `Scalar(67,255,255)` |
| 청록 (Cyan) | 🩵 | 165°~195° | **82 ~ 97** | `Scalar(82,50,50)` ~ `Scalar(97,255,255)` |
| 하늘/밝은파랑 (Sky) | 🔵 | 195°~215° | **97 ~ 107** | `Scalar(97,50,50)` ~ `Scalar(107,255,255)` |
| 파랑 (Blue) | 🔵 | 215°~255° | **107 ~ 127** | `Scalar(107,50,50)` ~ `Scalar(127,255,255)` |
| 남색 (Indigo) | 🟣 | 255°~270° | **127 ~ 135** | `Scalar(127,50,50)` ~ `Scalar(135,255,255)` |
| 보라 (Violet) | 🟣 | 270°~300° | **135 ~ 150** | `Scalar(135,50,50)` ~ `Scalar(150,255,255)` |
| 자주/분홍 (Magenta) | 🩷 | 300°~350° | **150 ~ 175** | `Scalar(150,50,50)` ~ `Scalar(175,255,255)` |

> ⚠️ **빨강은 색상환 0°/360° 경계에 걸쳐 있어 두 구간으로 나눠 처리한 뒤 합산해야 한다.**

```
색상환 (Hue wheel, OpenCV H 값):

         0 / 179
       빨강 (Red)
    ↗              ↖
  175 자주          5 오렌지
  150 보라         22 노랑
  135 남색         38 연두
  127 파랑         52 초록
  107 하늘         67 초록
   97 청록 ────── 82 청록
```

**실용 팁: S·V 조건으로 검출 품질 향상**

| 목적 | S (채도) 하한 | V (명도) 하한 |
|------|--------------|--------------|
| 선명한 색만 검출 | ≥ 100 | ≥ 100 |
| 어두운 환경 포함 | ≥ 50 | ≥ 30 |
| 흰색 제외 | ≥ 30 | 무관 |
| 검정 제외 | 무관 | ≥ 30 |

---

### InRange 동작 원리

`Cv2.InRange(src, lower, upper, dst)`는 픽셀 단위로 아래 조건을 평가한다.

$$
dst(x,y) = \begin{cases} 255 & \text{if } lower \leq src(x,y) \leq upper \\ 0 & \text{otherwise} \end{cases}
$$

- 다채널 Mat에서는 **모든 채널 조건을 동시에 만족**해야 255가 된다.  
  예: `InRange(hsv, Scalar(8,50,50), Scalar(20,255,255))` → H AND S AND V 범위 모두 충족 시 255
- 단일 채널 H에만 적용할 때는 S·V 조건 없이 색조만 걸러낸다.

---

### BitwiseAnd + 마스크 적용 원리

$$
dst(x,y) = \begin{cases} src1(x,y) \;\text{AND}\; src2(x,y) & \text{if } mask(x,y) = 255 \\ 0 & \text{if } mask(x,y) = 0 \end{cases}
$$

`src1 = src2 = hsv` 일 때 `hsv AND hsv = hsv` 이므로,  
**실질적으로 마스크가 255인 픽셀만 원본 값을 유지하고 나머지를 검정(0)으로 지운다.**

```
원본 HSV 이미지          마스크 (H_orange)        결과 (dst)
┌──────────────┐        ┌──────────────┐        ┌──────────────┐
│ 오렌지 영역  │  AND   │  255  255    │   =    │ 오렌지 영역  │
│ 배경(파랑)   │        │    0    0    │        │   0   0  0   │
└──────────────┘        └──────────────┘        └──────────────┘
```

## 배열 병합 

- [Array merge](.\Chapter5\Example-05-05-C#\Project\ProgramForWindow.csx)  

### 알파 블렌딩(alpha blending)

알파 블렌딩은 두 이미지를 **투명도(α) 비율에 따라 섞어 하나의 이미지로 합성**하는 기법이다.

$$
dst = src1 \times \alpha + src2 \times \beta + \gamma
$$

| 파라미터 | 의미 |
|----------|------|
| $\alpha$ | src1의 가중치 (기여도). 0.0 ~ 1.0 |
| $\beta$ | src2의 가중치 (기여도). 보통 $1 - \alpha$ 로 설정 |
| $\gamma$ | 밝기 보정값 (스칼라 오프셋). 보통 0.0 |

> $\alpha + \beta = 1.0$ 이 되도록 설정하면 전체 밝기가 유지된다.  
> $\alpha = 0.7,\ \beta = 0.3$ 이면 src1이 70%, src2가 30% 비율로 합성된다.

**직관적 이해:**

```
α = 1.0  →  src1만 보임  (src2 완전 투명)
α = 0.5  →  두 이미지가 반반 겹쳐 보임
α = 0.0  →  src2만 보임  (src1 완전 투명)
```

**OpenCV 사용 방법:**

```csharp
// Cv2.AddWeighted(src1, alpha, src2, beta, gamma, dst)
// 예: src1을 70%, src2를 30% 비율로 합성
Cv2.AddWeighted(src1, 0.7, src2, 0.3, 0.0, dst);
```

**활용 사례:**
- 워터마크(로고) 반투명 합성
- 슬라이드쇼 화면 전환 효과 (페이드 인/아웃)
- UI 오버레이 (반투명 HUD, 자막)
- 두 카메라 영상의 실시간 합성 

### gamma

`gamma`는 `AddWeighted` 수식에서 **모든 픽셀에 균일하게 더해지는 밝기 보정 상수**입니다.

$$
dst(x,y) = src1(x,y) \times \alpha + src2(x,y) \times \beta + \gamma
$$

| gamma 값 | 효과 |
|----------|------|
| `0.0` | 보정 없음 (일반적인 경우) |
| `양수 (+50)` | 결과 이미지 전체가 밝아짐 |
| `음수 (-50)` | 결과 이미지 전체가 어두워짐 |

픽셀값은 0~255로 클램핑되므로, gamma를 크게 올리면 밝은 영역이 255에서 포화(saturation)됩니다.

**실용적으로는 거의 `0.0`을 사용**하며, 합성 후 밝기를 의도적으로 조절해야 할 때만 설정합니다.

## 이진화 

- 동영상이나 이미지에서 어느 지점을 기준으로 픽셀을 분류해서 제외해야 할 때가 있다. 이때 특정 값을 기준으로 값이 높거나 낮은 픽셀을 검은색 또는 흰색의 값으로 변경한다.  

### 이진화를 하는 이유

컴퓨터 비전에서 이진화는 **이미지 분석의 전처리 단계**로, 다음과 같은 목적에서 사용된다.

#### 1. 정보 단순화 — 관심 영역과 배경 분리

그레이스케일 이미지는 0~255의 256단계 밝기를 가지지만, 대부분의 분석에서는  
**"전경(object)인가 / 배경(background)인가"** 라는 두 가지 정보만 필요하다.

```
그레이스케일 이미지          이진화 결과
┌──────────────┐           ┌──────────────┐
│ 120 200 210  │  thresh   │  0  255 255  │
│  80  90 230  │  = 127    │  0   0  255  │
│  40 150 190  │  ──────►  │  0  255 255  │
└──────────────┘           └──────────────┘
```

#### 2. 연산 속도 향상

픽셀 값이 0 또는 255(1비트 정보)이므로 이후 처리(윤곽선 검출, 모폴로지 연산 등)의  
연산량이 크게 줄어든다.

#### 3. 노이즈 제거

임계값 이하의 미세한 잡음 픽셀을 0으로 제거하여 이후 분석의 정확도를 높인다.

#### 4. 후속 알고리즘의 입력 조건

아래 알고리즘들은 이진 이미지를 입력으로 요구하거나 이진 이미지에서 훨씬 잘 동작한다.

| 후속 알고리즘 | 이진화가 필요한 이유 |
|--------------|-------------------|
| 윤곽선 검출 (`FindContours`) | 전경/배경 경계를 명확히 구분해야 함 |
| 모폴로지 연산 (`Erode`, `Dilate`) | 0/255 픽셀 기반으로 구조 요소 적용 |
| 레이블링 (`ConnectedComponents`) | 연결된 흰색 영역을 개별 객체로 분류 |
| 허프 변환 (`HoughLines`) | 엣지 이미지(이진)를 입력으로 사용 |
| 거리 변환 (`DistanceTransform`) | 0 픽셀로부터의 거리 계산 |

#### 5. 조명 변화에 대한 강인성

절대적인 픽셀 밝기 대신 **임계값 기준의 상대적 분류**를 사용하므로,  
Otsu 같은 자동 임계값 방법과 결합하면 조명이 바뀌어도 안정적으로 객체를 추출할 수 있다.

> **요약:** 이진화는 "어디가 물체고 어디가 배경인가"를 0과 255로 명확히 분리하는 작업으로,  
> 이후 모든 형태 분석·객체 검출의 기반이 된다.

- [이진화](.\Chapter5\Example-05-07-C#\Project\ProgramForWindow.csx)


## 오츠 알고리즘 

> 출처: Nobuyuki Otsu, "A Threshold Selection Method from Gray-Level Histograms", IEEE Transactions on Systems, Man, and Cybernetics, 1979.

### 핵심 아이디어

**임계값 T를 기준으로 픽셀을 두 그룹(배경·전경)으로 나눌 때, 두 그룹 간 밝기 차이가 가장 커지는 T를 자동으로 찾는다.**

```
히스토그램 (이중 봉우리, bimodal)

빈도
 │    배경          전경
 │   ╭──╮         ╭──╮
 │  ╭╯  ╰╮       ╭╯  ╰╮
 │ ╭╯    ╰───────╯    ╰╮
 └────────────────────────→ 밝기 (0~255)
           ↑
         최적 T  ← 오츠가 찾는 지점 (두 봉우리 사이 골짜기)
```

### 수식

**클래스 간 분산(between-class variance)을 최대화하는 T를 탐색:**

$$
\sigma^2_B(T) = w_0(T) \cdot w_1(T) \cdot \left[\mu_0(T) - \mu_1(T)\right]^2
$$

| 기호 | 의미 |
|------|------|
| $w_0(T)$ | T 이하 픽셀 비율 (배경 가중치) |
| $w_1(T)$ | T 초과 픽셀 비율 (전경 가중치) |
| $\mu_0(T)$ | T 이하 픽셀의 평균 밝기 |
| $\mu_1(T)$ | T 초과 픽셀의 평균 밝기 |

$$
T^* = \underset{T}{\arg\max}\ \sigma^2_B(T)
$$

### OpenCV 사용법

```csharp
// thresh 파라미터(127)는 무시됨 → 자동 계산된 임계값이 반환됨
// 반드시 Binary 또는 BinaryInv 와 OR 조합
double autoThresh = Cv2.Threshold(gray, binary, 0, 255,
    ThresholdTypes.Binary | ThresholdTypes.Otsu);
Console.WriteLine($"오츠 임계값: {autoThresh}");
```

### 적합한 상황 / 한계

| 적합 | 부적합 |
|------|--------|
| 히스토그램이 이중 봉우리(배경·전경 명확) | 단일 봉우리 히스토그램 |
| 조명이 전체적으로 균일 | 조명이 지역적으로 불균일 |
| 배경과 물체의 밝기 차이가 큼 | 노이즈가 매우 많은 이미지 |

---

## 삼각형 알고리즘

> 출처: Zack, G.W., Rogers, W.E. and Latt, S.A., "Automatic measurement of sister chromatid exchange frequency", Journal of Histochemistry & Cytochemistry, 1977.

### 핵심 아이디어

**히스토그램의 최고점(peak)과 양 끝점을 잇는 직선에서 가장 멀리 떨어진 지점을 임계값으로 선택한다.**

```
히스토그램 (단일 봉우리, unimodal)

빈도
 │        ╭─╮
 │       ╭╯ ╰╮
 │      ╭╯   ╰╮
 │  ───╭╯     ╰──────────
 └──────────────────────────→ 밝기 (0~255)
   최소    ↑ peak     최대
           │  \
           │    \  ← peak와 최솟값 끝을 잇는 직선
           │      \
    T* ────┤        \  ← 이 직선으로부터 가장 먼 지점 = T*
```

### 수식

peak 좌표를 $(x_p, y_p)$, 히스토그램 끝점을 $(x_e, 0)$ 이라 할 때,  
직선 $\overline{peak \to end}$ 으로부터 각 점까지의 수직 거리 $d(T)$ 를 최대화:

$$
T^* = \underset{T}{\arg\max}\ d(T) = \underset{T}{\arg\max}\ \frac{|y_p \cdot T - (x_p - x_e) \cdot h(T) - y_p \cdot x_e|}{\sqrt{y_p^2 + (x_p - x_e)^2}}
$$

> $h(T)$: 밝기 T에서의 히스토그램 빈도값

### OpenCV 사용법

```csharp
// 반드시 Binary 또는 BinaryInv 와 OR 조합
double autoThresh = Cv2.Threshold(gray, binary, 0, 255,
    ThresholdTypes.Binary | ThresholdTypes.Triangle);
Console.WriteLine($"삼각형 임계값: {autoThresh}");
```

### 오츠 vs 삼각형 비교

| | 오츠 | 삼각형 |
|--|------|--------|
| 히스토그램 가정 | 이중 봉우리 (bimodal) | 단일 봉우리 (unimodal) |
| 적합한 이미지 | 배경·전경이 명확히 분리 | 한쪽에 몰린 밝기 분포 |
| 예시 | 문서 스캔, 흑백 물체 | 현미경 이미지, 형광 이미지 |
| 계산 복잡도 | O(256) | O(256) |

---

## 적응형 이진화 알고리즘 

> 전역(global) 임계값 방식(오츠, 삼각형)은 이미지 전체에 하나의 T를 적용한다.  
> **적응형(Adaptive) 이진화**는 픽셀마다 주변 영역의 밝기를 기반으로 **지역(local) 임계값**을 동적으로 계산한다.

### 핵심 아이디어

```
전역 이진화 (조명 불균일 시 문제 발생)
┌────────────────────────┐
│  어두운쪽  │  밝은쪽  │   단일 T로는 어두운 쪽이 모두 0이 됨
│  글자 사라짐  │  OK    │
└────────────────────────┘

적응형 이진화 (각 블록마다 T 계산)
┌──────┬──────┬──────┐
│ T=80 │T=120 │T=160 │   각 영역에 맞는 T → 균일한 이진화 결과
│  OK  │  OK  │  OK  │
└──────┴──────┴──────┘
```

### 수식

각 픽셀 $(x, y)$ 의 임계값:

$$
T(x, y) = \mu_{local}(x,y) - C
$$

| 기호 | 의미 |
|------|------|
| $\mu_{local}$ | 픽셀 $(x,y)$ 주변 블록(blockSize × blockSize)의 평균 또는 가우시안 가중 평균 |
| $C$ | 미세 조정 상수 (보통 양수; 작을수록 더 많은 픽셀이 전경으로 분류) |

### 지역 평균 계산 방식 (AdaptiveThresholdTypes)

| 방식 | 수식 | 특징 |
|------|------|------|
| `MeanC` | $\mu = \dfrac{1}{N}\sum_{i,j} I(i,j)$ | 블록 내 단순 평균. 빠름 |
| `GaussianC` | $\mu = \sum_{i,j} G(i,j) \cdot I(i,j)$ | 중심 픽셀에 가우시안 가중치. 경계가 부드럽고 노이즈에 강함 |

### OpenCV 사용법

```csharp
// Cv2.AdaptiveThreshold(src, dst, maxValue, adaptiveMethod, thresholdType, blockSize, C)
//   adaptiveMethod : AdaptiveThresholdTypes.MeanC 또는 GaussianC
//   blockSize      : 지역 임계값 계산에 사용할 블록 크기 (홀수여야 함, 예: 11, 21)
//   C              : 평균에서 뺄 상수 (보통 2~10)
Cv2.AdaptiveThreshold(gray, binary, 255,
    AdaptiveThresholdTypes.GaussianC,
    ThresholdTypes.Binary,
    blockSize: 11,
    c: 2);
```

### 전역 vs 적응형 이진화 비교

| | 전역 (Otsu/Triangle) | 적응형 (Adaptive) |
|--|----------------------|-------------------|
| 임계값 수 | 이미지 전체에 1개 | 픽셀마다 다름 |
| 조명 균일 시 | 빠르고 정확 | 불필요하게 복잡 |
| 조명 불균일 시 | 일부 영역 소실 | 안정적 |
| 연산 비용 | 낮음 | 높음 (blockSize²만큼 연산) |
| 적합 사례 | 스캔 문서, 단순 배경 | 실외 촬영, 그림자, 조명 편차 |

- [적응형 이진화 알고리즘](.\Chapter5\Example-05-09-C#\Project\ProgramForWindow.csx)  