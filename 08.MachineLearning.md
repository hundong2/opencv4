## 머신 러닝 (Machine Learning )

- 데이터를 기반으로 컴퓨터를 프로그래밍하는 연구 분야 

- $$ 인공지능 \supset 머신러닝 \supset 딥 러닝 $$

### k-means algorithm  

- 이미지에서 색을 분류하여 표시 (색양자화 - Color Quantization)  
- [Example code](.\Chapter8\Example-08-01-C#\Project\ProgramForWindow.csx)  

#### 코드 설명 

**'이미지(2D)' 데이터를 머신러닝 알고리즘인 K-Means가 이해할 수 있는 '수학적 통계 데이터(1D)'로 변환했다가, 다시 '이미지(2D)'로 복구하는 과정**

---

### 1. 이미지를 왜 1차원으로 길게 펼칠까요? (`Reshape`)

```csharp
src.Reshape(3, src.Width * src.Height)

```

* **이유:** K-Means는 '이미지'가 무엇인지 모릅니다. 단지 **수많은 데이터(샘플)들의 목록**으로만 이해합니다.
* 우리가 보는 이미지는 가로(X), 세로(Y) 좌표를 가진 2차원 공간이지만, K-Means 알고리즘에게는 픽셀의 위치(x, y)는 중요하지 않습니다. 오직 "이 픽셀의 색상(B, G, R)이 무엇인가?"만 중요합니다.
* 따라서 `너비 x 높이` 형태의 사각형 격자를, `총 픽셀 수 x 1` 형태의 기나긴 1차원 줄(List)로 쭉 펼쳐서 알고리즘에 던져주기 위해 `Reshape`을 사용합니다.

### 2. 색상 값을 왜 소수점(Float)으로 바꿀까요? (`ConvertTo`)

```csharp
.ConvertTo(data, MatType.CV_32FC3);

```

* **이유:** K-Means는 내부적으로 평균(Mean)을 내고 거리(Distance)를 계산하는 알고리즘이기 때문입니다.
* 색상이 얼마나 비슷한지 계산하려면 두 색상 사이의 3차원 유클리드 거리를 구해야 합니다: $\sqrt{(B_1-B_2)^2 + (G_1-G_2)^2 + (R_1-R_2)^2}$
* 원래 이미지의 색상 값은 0~255 사이의 정수(8-bit)입니다. 정수 상태로 수만 개의 픽셀 값을 더하고 나누다 보면 소수점 이하가 버려지거나(정밀도 하락), 값이 255를 넘어 폭발(Overflow)할 수 있습니다.
* 또한 OpenCV의 `cv::kmeans` 함수 자체가 설계될 때 무조건 32비트 실수형(`CV_32F`) 데이터만 입력받도록 강제되어 있습니다.

### 3. K-Means를 왜 10번이나 반복 시킬까요? (`attempts: 10`)

```csharp
double retval = Cv2.Kmeans(data, K, bestLabels, TermCriteria.Both(10, 0.001), 10, KMeansFlags.RandomCenters, centers);

```

* **이유:** 초기 뽑기(Random) 운에 따라 결과가 망가질 수 있기 때문입니다.
* K-Means 알고리즘은 처음에 K개의 대표 색상(중심점)을 아무렇게나 무작위(RandomCenters)로 찍고 시작합니다. 그런데 이 초기 중심점이 우연히 너무 비슷한 색상 쪽에만 몰려있으면, 최적의 대표 색상을 찾지 못하고 엉뚱한 결과(Local Minimum)에 빠지게 됩니다.
* 그래서 매개변수에 `10`을 넣어서 "처음부터 다시 무작위로 찍고 그룹화하는 과정을 완전히 독립적으로 10번 시도해 봐. 그리고 그중에서 가장 군집화가 잘 된(픽셀들이 중심에 가장 오밀조밀하게 모인) 최적의 결과를 하나만 줘!"라고 지시하는 것입니다.

### 4. 왜 굳이 반복문(`for`)으로 픽셀을 하나하나 다시 칠할까요?

```csharp
for (int y = 0; y < dst.Height; y++) { ... }

```

* **이유:** K-Means의 결과물은 완성된 이미지가 아니라 "정답지(Labels)"와 "물감(Centers)"이기 때문입니다.
* `bestLabels`는 단순히 "0번 픽셀은 3번 그룹, 1번 픽셀은 0번 그룹..." 식의 번호표입니다.
* `centers`는 "0번 그룹의 색상은 빨강, 1번 그룹은 파랑..." 식의 팔레트(물감)입니다.
* 이제 1차원으로 길게 펼쳤던 번호표(`idx`)를 보면서, 2차원 빈 도화지(`dst`)의 원래 위치(`x, y`)에 팔레트(`centers`)에서 물감을 찍어다 칠하는 조립 과정이 필요합니다. 이것이 바로 마지막 이중 `for` 문의 역할입니다.

### 설명

- k-means 알고리즘은 특별한 훈력 방식이 존재하지 않는다. 
- 인스턴스 기반의 학습(instance-Based learning)
- 게이른 학습(Lazy Model)으로 부른다.  

- [temp data](https://github.com/zalandoresearch/fashion-mnist/tree/master/data/fashion)  

- [k-means](.\Chapter8\Example-08-03-C#\Project\ProgramForWindow.csx)   


### OpenCvSharp.ML

- k-means algorithm 사용을 위한 namespace 
- [K-means algorithm](.\Chapter8\Example-08-09-C#\Project\ProgramForWindow.csx)  


#### SVM (Support Vector Machine, SVM)

- K-means 이웃 알고리즘과 마찬가지로 분류와 회귀 분석에서 사용되는 알고리즘 
- 모든 훈련 데이터와 거리를 계싼해 지정한 k개에서 가장 많은 최소거리를 갖는 클래스로 분류 

### DNN

#### Blob & CaffeModel

- [CaffeModel Example](.\Chapter8\Example-08-15-C#\Project\ProgramForWindow.csx) 

prob은 임의 문자열이 아니라 모델 안에 정의된 출력 레이어 이름입니다.

의미를 간단히 정리하면:
1. GoogLeNet(Caffe)에서 prob는 보통 softmax 확률 출력 레이어입니다.
2. net.Forward("prob")는 그 레이어의 결과를 가져오라는 뜻입니다.
3. 결과는 각 클래스(예: ImageNet 1000개)에 대한 확률 벡터이고, 합이 1에 가깝습니다.
4. 지금 코드의 MinMaxLoc은 그중 가장 큰 확률의 인덱스를 찾아 1등 클래스를 출력합니다.

중요한 점:
- 모든 모델이 출력 이름을 prob로 쓰는 건 아닙니다.
- 모델마다 output, softmax, predictions처럼 이름이 다를 수 있습니다.
- 그래서 다른 모델로 바꾸면 Forward에 넣는 레이어 이름도 해당 모델에 맞게 바꿔야 합니다.


## yolov3 darknet 

- [darknet yolov3 image detection](.\Chapter8\Example-08-17-C#\Project\ProgramForWindow.csx)  
- [coco.names download](https://github.com/pjreddie/darknet/blob/master/data/coco.names)  
- [yolov3.cfg and weights download](https://pjreddie.com/darknet/yolo/)  

### DNN

DNN 입력 텐서는 모델이 먹을 수 있는 형태로 바꾼 “숫자 배열”입니다.  
지금 코드 기준으로는 이 줄이 핵심입니다.

- ProgramForWindow.csx에서 `CvDnn.BlobFromImage(...)`

여기서 만들어지는 텐서는 보통 NCHW 형태입니다.

- N: 배치 크기 (몇 장을 한 번에 넣는지, 여기선 1)
- C: 채널 수 (RGB/BGR의 3채널)
- H: 높이
- W: 너비

즉 현재 설정은 대략 `(1, 3, 416, 416)` 크기라고 보면 됩니다.

왜 변환이 필요하냐면:
- 원본 이미지는 픽셀 범위가 보통 0~255이고 크기도 제각각
- 모델은 학습 때 쓰던 고정 크기/스케일/채널 순서를 기대
- 그래서 `BlobFromImage`가 리사이즈, 정규화, 채널/배열 재배치를 한 번에 처리

현재 코드의 각 인자 의미:
- `1/255f`: 픽셀값을 0~1로 정규화
- `new Size(416, 416)`: 모델 입력 크기로 리사이즈
- `crop:false`: 비율 유지 크롭 대신 그냥 맞춰서 스케일
- (기본) `swapRB:false`: BGR 순서 유지  
  필요 시 `swapRB:true`로 RGB로 바꿀 수 있음

주의:
- 모델이 기대하는 전처리와 다르면 정확도가 크게 떨어집니다.
- 예: 어떤 모델은 640x640, 어떤 모델은 RGB, 어떤 모델은 mean/std 정규화를 요구합니다.

한 줄 요약:
- DNN 입력 텐서는 “이미지를 모델 규격(NCHW, 크기, 스케일, 채널 순서)에 맞춘 수치 데이터”입니다.  
- 이 코드에서는 `BlobFromImage`가 그 작업을 담당합니다.

### Mat Class 상세 정리 (OpenCvSharp 기준)

`Mat`은 OpenCV에서 가장 중요한 자료형입니다.  
이미지뿐 아니라, 좌표/특징점/딥러닝 출력 같은 **행렬 데이터 전체**를 담는 컨테이너입니다.

#### 1) Mat을 한 줄로 이해하기

- `Mat = (픽셀 데이터 메모리) + (행/열/채널/타입 정보 헤더)`
- 우리가 화면에서 보는 사진도 결국 숫자 행렬이며, OpenCV는 이를 `Mat`으로 다룹니다.
- 예를 들어 컬러 이미지는 보통 `CV_8UC3` 타입입니다.
  - `8U`: 8비트 부호 없는 정수 (0~255)
  - `C3`: 채널 3개 (B, G, R)

#### 2) 자주 보는 타입 표기 읽는 법

- `CV_8UC1`: 그레이스케일(1채널)
- `CV_8UC3`: 컬러 BGR(3채널)
- `CV_32FC1`: 32비트 실수 1채널
- `CV_32FC3`: 32비트 실수 3채널

딥러닝 전처리, 통계 계산, K-Means/SVM 입력에서는 `CV_32F` 계열을 자주 사용합니다.

#### 3) 생성 방법 (기초)

```csharp
// 파일에서 읽기
Mat img = Cv2.ImRead("cat.jpg");

// 빈 행렬 생성: 높이 480, 너비 640, 8비트 3채널
Mat canvas = new Mat(480, 640, MatType.CV_8UC3, Scalar.Black);

// 기존 Mat과 같은 크기/타입으로 0으로 초기화
Mat zeros = Mat.Zeros(img.Size(), img.Type());
```

#### 4) 핵심 속성/메서드

- `Rows`, `Cols`: 높이, 너비
- `Channels()`: 채널 수
- `Type()`: 데이터 타입
- `Size()`: 크기(`Width`, `Height`)
- `Clone()`: 데이터까지 깊은 복사
- `CopyTo(dst)`: 대상 Mat으로 데이터 복사
- `Reshape(...)`: 모양만 재해석(데이터 재배치 없이 헤더 관점 변경)
- `ConvertTo(...)`: 타입 변환(예: 8U -> 32F)

#### 5) Clone vs 얕은 복사 (중요)

```csharp
Mat a = Cv2.ImRead("cat.jpg");
Mat b = a;          // 얕은 복사: 같은 데이터 메모리를 가리킴
Mat c = a.Clone();  // 깊은 복사: 데이터 별도 복제
```

- `b`를 수정하면 `a`도 같이 바뀔 수 있습니다.
- 원본 보존이 필요하면 `Clone()`을 사용하는 습관이 안전합니다.

#### 6) 픽셀 접근 방법

```csharp
Vec3b color = img.At<Vec3b>(y, x); // B,G,R 순서
byte b = color.Item0;
byte g = color.Item1;
byte r = color.Item2;
```

- OpenCV 기본 채널 순서는 **RGB가 아니라 BGR**입니다.
- 반복문으로 모든 픽셀을 직접 읽고 쓰는 방식은 직관적이지만 느릴 수 있습니다.
- 가능하면 OpenCV 함수(벡터화 연산)를 우선 사용하세요.

#### 7) Mat과 DNN/ML 연결 포인트

- `CvDnn.BlobFromImage(...)` 입력: `Mat` 이미지
- `Cv2.Kmeans(...)` 입력: 보통 `CV_32F` 형태의 `Mat`
- `net.Forward(...)` 출력도 `Mat`(또는 `Mat[]`)로 받습니다.

즉, OpenCV에서 전처리 -> 추론 -> 후처리 전 과정이 대부분 `Mat` 중심으로 연결됩니다.

#### 8) 자주 하는 실수

- 타입 불일치: `CV_8U` 상태로 ML 함수 호출
- 채널 순서 혼동: RGB로 생각하고 BGR 데이터를 그대로 사용
- 입력 크기 불일치: 모델이 기대하는 크기(예: 416x416) 미적용
- 깊은 복사 누락: 참조 공유로 원본이 의도치 않게 변경

#### 9) 실무 체크리스트

1. 현재 `Mat`의 `Type()`, `Rows`, `Cols`, `Channels()`를 먼저 확인한다.
2. 모델/알고리즘 요구 타입으로 `ConvertTo`를 적용한다.
3. 색공간이 맞는지(`BGR`, `RGB`, `GRAY`)를 확인한다.
4. 원본 보존이 필요하면 `Clone()` 후 처리한다.


## 텐서플로

- [텐서플로](.\Chapter8\Example-08-13-C#\Project\ProgramForWindow.csx)  
